#pragma once

// UI Font Size Constants
namespace ui_font_sizes {

    // Main info text (enemies count, special rules)
    constexpr float INFO_TEXT_SIZE = 24.f;

    // Deck counter text
    constexpr float DECK_COUNTER_SIZE = 20.f;

} // namespace ui_font_sizes
